@model ShoppingCartViewModel

@if(Model.ShoppingCartItem.Count > 0)
{
    <li>
        <a asp-controller="ShoppingCart" asp-action="Index">
            <span class="glyphicon glyphicon-shopping-cart"></span>
            <span id="cart-status">
                @Model.ShoppingCartItem.Count
            </span>
        </a>
    </li>
}